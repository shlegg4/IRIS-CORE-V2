function(iris_test name source)
  add_executable(${name} ${source})
  target_link_libraries(${name} PRIVATE iris::iris)
  target_include_directories(${name} PRIVATE ${PROJECT_SOURCE_DIR}/src)
  add_test(NAME ${name} COMMAND ${name})
  set_tests_properties(${name} PROPERTIES LABELS "unit")
endfunction()
iris_test(channel_tests pipeline/ChannelTests.cpp)
iris_test(capture_clock_tests capture/CaptureClockTests.cpp)
iris_test(frame_synchronizer_tests capture/FrameSynchronizerTests.cpp)
iris_test(metrics_tests infrastructure/metrics/MetricsTests.cpp)
iris_test(output_stage_tests stages/OutputStageTests.cpp)
iris_test(runtime_control_tests runtime/RuntimeControlTests.cpp)

if(IRIS_BUILD_HARDWARE_TESTS)
  add_subdirectory(hardware)
endif()
